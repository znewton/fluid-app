plugins:
    - path: .yarn/plugins/@yarnpkg/plugin-typescript.cjs
      spec: "@yarnpkg/plugin-typescript"

yarnPath: .yarn/releases/yarn-berry.cjs

# npmScopes:
#   fluidframework:
#     npmRegistryServer: http://localhost:4873
#     npmAlwaysAuth: false

unsafeHttpWhitelist:
  - "localhost"

nodeLinker: node-modules

npmScopes:
  fluidframework:
    npmRegistryServer: "https://offnet.pkgs.visualstudio.com/officenet/_packaging/fluid/npm/registry/"
    npmAuthIdent: "${NPM_AUTH_IDENT}"
    npmAlwaysAuth: true
